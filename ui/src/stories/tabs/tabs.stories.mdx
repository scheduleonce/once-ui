import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import {
  MatTabsModule,
  MatTab
} from '../../components/tabs/module';
import { MatTabStorybook } from './tabs.component';
import { BrowserAnimationsModule } from '@angular/platform-browser/animations';
import { action } from '@storybook/addon-actions';
import { THEME } from '../const';

<Meta title="MatTab" component={MatTab} />

# Card

<Canvas>
  <Story
    name="regular"
    parameters={{
      docs: {
        source: {
          code: `
            <mat-tab-group>
              <mat-tab label="First"> Content 1 </mat-tab>
              <mat-tab label="Second"> Content 2 </mat-tab>
              <mat-tab label="Third"> Content 3 </mat-tab>
            </mat-tab-group>`,
        },
      },
    }}
    args={{
      theme: THEME[0]
    }}
    argTypes={{
      theme: {
        options: THEME,
        control: { type: 'select' },
      }
    }}
  >
    {(props) => ({
      moduleMetadata: {
        imports: [MatTabsModule, BrowserAnimationsModule],
        schemas: [],
        declarations: [MatTabStorybook],
      },
      template: `<mat-tab-storybook ngClass="{{theme}}"></mat-tab-storybook>`,
      props: {...props},
    })}
  </Story>
</Canvas>

<ArgsTable of={MatTabsModule} />
