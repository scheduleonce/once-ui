import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { OuiTabsModule, OuiTab } from '../../components';
import { COLORS, THEME } from '../const';
import { action } from '@storybook/addon-actions';
import { OuiTabStorybook } from './tab.component';
import { BrowserAnimationsModule } from '@angular/platform-browser/animations';

<Meta title="tabs" />

# tabs

<Canvas>
  <Story
    name="tabs"
    height="700px"
    parameters={{
      docs: {
        source: { code: `<oui-tab>hello</oui-tab>` },
      },
    }}
  >
    {(props) => ({
      moduleMetadata: {
        imports: [OuiTabsModule, BrowserAnimationsModule],
        schemas: [],
        declarations: [OuiTabStorybook],
      },
      template: `<oui-tab></oui-tab>`,
      props: { ...props, clicked: action('click') },
    })}
  </Story>
</Canvas>

<ArgsTable of={OuiTabsModule} />
