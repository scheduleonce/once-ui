import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { OuiIconModule, Icon } from '../../components';
import { action } from '@storybook/addon-actions';
import { LABELPOSITION, COLORS, ICON_COLORS } from '../const';
import { OuiiconStorybook } from './icon.component';

<Meta title="Icon" />

# Icon

OuiIcon component to display an icon. It can be used in the following ways:

Specify the svgIcon input to load an SVG icon from a URL previously registered with the addSvgIcon, addSvgIconSet. Examples: `<oui-icon svgIcon="[name]"></oui-icon> <oui-icon svgIcon="building"></oui-icon>`. See https://github.com/scheduleonce/once-ui/blob/master/ui/src/components/icon/README.md

Icon with Default color:

<Canvas>
  <Story
    name="Default color"
    parameters={{
      docs: {
        source: {
          code: `<oui-icon svgIcon="calendar" color="primary"></oui-icon>`,
        },
      },
    }}
    args={{
      size: 20,
      icon: 'calendar',
    }}
    argTypes={{
      size: {
        control: {
          type: 'number'
        }
      }
    }}
  >
    {props => ({
      moduleMetadata: {
        imports: [OuiIconModule],
        schemas: [],
        declarations: [OuiiconStorybook],
      },
      template: `<oui-icon-storybook 
                [icon]="icon" 
                [size]="size">
                </oui-icon-storybook>`,
      props,
    })}
  </Story>
</Canvas>

You can change size and use specific colors on `Icon` component by using css:

<Canvas>
  <Story
    name="Custom icon"
    parameters={{
      docs: {
        source: {
          code: `<oui-icon svgIcon="configuration" class="maroon"></oui-icon>`,
        },
      },
    }}
    args={{
      size: 20,
      color: COLORS[0],
      icon: 'configuration',
    }}
    argTypes={{
      color: {
        options: ICON_COLORS,
        control: { type: 'select' },
      },
      size: {
        control: {
          type: 'number'
        }
      }
    }}
  >
    {props => ({
      moduleMetadata: {
        imports: [OuiIconModule],
        schemas: [],
        declarations: [OuiiconStorybook],
      },
      template: `<oui-icon-storybook 
                [color]="color" 
                [icon]="icon" 
                [size]="size">
                </oui-icon-storybook>`,
      props,
    })}
  </Story>
</Canvas>

Stackblitz demo link : https://stackblitz.com/edit/oui-icons

<ArgsTable of={Icon} />
